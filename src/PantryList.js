import { Text, View } from "react-native";

function PantryListPage() {
    return (
      <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
        <Text>Settings!</Text>
      </View>
    );
  }

  export default PantryListPage;